version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: netops
      POSTGRES_PASSWORD: netops_password
      POSTGRES_DB: netops
    ports:
      - "5432:5432"
  redis:
    image: redis:7
    ports:
      - "6379:6379"
